export const metadata = {
  title: "BLOCKTRUST TrustScan — Outlook",
  robots: { index: false, follow: false },
};

export default function OutlookLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
